$kind: http-request
description: >-
  Get the patient's emotional evolution over time.
  Returns aggregated emotional metrics, trends, and charts data.

  **Query parameters:**
  - `days` (integer, optional): Number of days to include (default: 30)
  - `metric` (string, optional): `sentiment`, `anxiety`, `mood` (default: all)

  **Auth:** JWT Bearer (IsAuthenticated - Patient only)
url: "{{base_url}}/api/analysis/evolution/"
method: GET
order: 3030
