$kind: http-request
description: >-
  Export all journal entries for the authenticated patient as PDF or JSON.

  **Query parameters:**
  - `format` (string, optional): `pdf` or `json` (default: pdf)

  **Auth:** JWT Bearer (IsAuthenticated - Patient only)
url: "{{base_url}}/api/entries/export/"
method: GET
order: 1060
