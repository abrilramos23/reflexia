$kind: http-request
description: >-
  Add a private note to a patient (therapist only).

  **Request body:**
  - `content` (string): Private note visible only to the therapist

  **Auth:** JWT Bearer (IsAuthenticated - Therapist with access to patient)
url: "{{base_url}}/api/entries/patients/{{patient_id}}/notes/"
method: POST
order: 2030
body:
  mode: raw
  raw: |
    {
      "content": "Patient showing signs of anxiety improvement. Sleep quality still a concern. Consider discussing coping strategies."
    }
