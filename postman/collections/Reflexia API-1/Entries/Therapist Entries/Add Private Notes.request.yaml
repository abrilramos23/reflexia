$kind: http-request
description: >-
  Add or update private notes on a patient's entry (therapist only).

  **Request body:**
  - `notes` (string): Private notes visible only to the therapist

  **Auth:** JWT Bearer (IsAuthenticated - Therapist with access to patient)
url: "{{base_url}}/api/entries/patients/{{patient_id}}/{{entry_id}}/notes/"
method: POST
order: 2030
body:
  mode: raw
  raw: |
    {
      "notes": "Patient showing signs of anxiety improvement. Sleep quality still a concern. Consider discussing coping strategies."
    }
