$kind: http-request
description: >-
  Create a new journal entry.

  **Request body:**
  - `content` (string, required): Entry text
  - `emotion_dimension` (integer, optional): Primary emotion dimension (1-9)

  **Auth:** JWT Bearer (IsAuthenticated - Patient only)
url: "{{base_url}}/api/entries/"
method: POST
order: 1020
body:
  mode: raw
  raw: |
    {
      "content": "Today was a challenging day. I felt anxious about the upcoming presentation at work. I tried breathing exercises which helped temporarily.",
      "emotion_dimension": 7
    }
