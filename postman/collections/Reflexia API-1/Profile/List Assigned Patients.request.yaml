$kind: http-request
description: |-
  List all patients assigned to the authenticated therapist.

  **Auth:** JWT Bearer (IsTherapistUser)
url: "{{base_url}}/api/users/patients/"
method: GET
order: 5000
