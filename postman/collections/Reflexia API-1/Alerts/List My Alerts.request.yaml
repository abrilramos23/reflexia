$kind: http-request
description: >-
  List clinical alerts affecting the authenticated patient.
  Shows alerts about the patient that they are authorized to see.

  **Query parameters:**
  - `status` (string, optional): `open`, `validated`, `dismissed`, `resolved`
  - `ordering` (string, optional): `-created_at` for newest first

  **Auth:** JWT Bearer (IsAuthenticated - Patient only)
url: "{{base_url}}/api/alerts/my-alerts/"
method: GET
order: 5060
