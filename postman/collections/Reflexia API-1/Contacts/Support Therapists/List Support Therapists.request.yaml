$kind: http-request
description: |-
  List all support therapists linked to the authenticated therapist.

  **Auth:** JWT Bearer (IsTherapistUser)
url: "{{base_url}}/api/contacts/support-therapists/"
method: GET
order: 1000
